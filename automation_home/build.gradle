jar {
    manifest {
        attributes 'Main-Class': 'nl.gingerbeard.domotica.MainClass'
    }
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

dependencies {
	compile project(':automation_framework')
}